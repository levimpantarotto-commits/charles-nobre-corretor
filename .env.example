NEXT_PUBLIC_SUPABASE_URL=""
NEXT_PUBLIC_SUPABASE_ANON_KEY=""

# Service role key do Supabase. Server-only. Habilita writes (bypassa RLS).
SUPABASE_SERVICE_ROLE_KEY=""

# Credenciais admin (server-only). Nunca prefixe com NEXT_PUBLIC_.
ADMIN_LEVI_EMAIL=""
ADMIN_LEVI_PASS=""
ADMIN_CHARLES_EMAIL=""
ADMIN_CHARLES_PASS=""

# Token de sessão (use `node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"`)
ADMIN_SESSION_TOKEN=""
